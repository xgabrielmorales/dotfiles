#!/usr/bin/env bash

set -euo pipefail

cd "$HOME/Pictures/Screenshots"

for f in ss.*T*.png; do
    [ -f "$f" ] || continue
    day=${f#ss.}
    day=${day%%T*}
    [[ "$day" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]] || continue
    mkdir -p "$day"
    mv -n -- "$f" "$day/"
done
