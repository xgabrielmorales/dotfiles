#!/usr/bin/env bash

set -euo pipefail

cd "$HOME/Downloads"

for f in *; do
    [ -e "$f" ] || continue
    [[ "$f" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]] && continue
    day=$(date -r "$f" +%Y-%m-%d)
    mkdir -p "$day"
    mv -n -- "$f" "$day/"
done
